{
  "name": "duo-wish-board-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "playwright:install": "playwright install chromium",
    "test:compose-detail": "npm run build && playwright test tests/visual-screenshots.spec.ts --grep \"compose|wish-detail|space\" && playwright test tests/compose-detail-acceptance.spec.ts",
    "test:home-list": "npm run build && playwright test tests/visual-screenshots.spec.ts --grep \"home|list\" && playwright test tests/home-list-acceptance.spec.ts",
    "test:screenshots": "npm run build && playwright test tests/visual-screenshots.spec.ts",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "pinia": "^3.0.4",
    "tslib": "^2.8.1",
    "vue": "^3.5.32",
    "vue-router": "^5.0.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^24.12.2",
    "@vitejs/plugin-vue": "^6.0.6",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vue-tsc": "^3.2.7"
  }
}
