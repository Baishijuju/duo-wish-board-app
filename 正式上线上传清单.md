# 正式上线上传清单

这份清单只针对当前这套 Vue + Supabase 正式联网版。

最重要的一句先放前面：

- 如果你准备把当前正式版上传到 GitHub 仓库，应该上传 `app/` 目录里面的内容作为 GitHub 仓库根目录内容。
- 不要把工作区根目录的归档内容和 `app/` 新版混着传到同一个正式仓库根目录；旧静态版已经移到 [../archive/2026-05-19-relaunch-cleanup/root-legacy-static/](../archive/2026-05-19-relaunch-cleanup/root-legacy-static/)。

## 你现在应该怎么理解目录

当前本地工作区现在是“正式版 + 归档”并存：

- 工作区根目录：说明文件和归档入口，旧静态版已经不再留在主干根目录。
- [../archive/2026-05-19-relaunch-cleanup/](../archive/2026-05-19-relaunch-cleanup/)：这次重新上线前移出的旧静态版原型、旧页面素材和临时产物。
- `app/`：现在真正要上线的 Vue 版双人共享应用。

所以你以后手动上传 GitHub 时，目标应该是下面这种映射：

- 本地 `app/src/` -> GitHub 仓库根目录 `src/`
- 本地 `app/public/` -> GitHub 仓库根目录 `public/`
- 本地 `app/package.json` -> GitHub 仓库根目录 `package.json`
- 本地 `app/index.html` -> GitHub 仓库根目录 `index.html`
- 本地 `app/.github/workflows/deploy-pages.yml` -> GitHub 仓库根目录 `.github/workflows/deploy-pages.yml`

不是下面这种：

- GitHub 仓库根目录里再保留一个 `app/` 子文件夹，同时部署平台又按根目录构建

只有在你明确打算让远端仓库保留 `app/` 这一层时，Cloudflare 或 Vercel 的 Root directory 才应该填 `app`。

## 建议上传到 GitHub 的内容

下面这些，默认应该上传：

- `.github/`
- `.gitignore`
- `src/`
- `public/`
- `supabase/`
- `index.html`
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `tsconfig.app.json`
- `tsconfig.node.json`
- `vite.config.ts`
- `vercel.json`
- `README.md`
- `.env.example`
- `.env.production`

说明：

- `supabase/` 不是前端运行必须上传给浏览器的内容，但它保存了数据库 migration，是这套项目的重要源代码，建议一起进 GitHub。
- `.env.production` 当前放的是公开前端配置，只包含前端可公开的 Supabase 地址和 publishable key，可以进仓库。
- 绝对不要把 Supabase service role key 之类的私密密钥写进前端仓库。

## 不要上传到 GitHub 的内容

下面这些不要传：

- `node_modules/`
- `dist/`
- `.vscode/`
- `.env.local`
- `ui-backups/`
- `archive/`

说明：

- `node_modules/` 和 `dist/` 都是可重新生成的，不需要进仓库。
- `.env.local` 是本机私有配置，不应该上传。
- `ui-backups/` 是这次改版前的本地恢复点，只用于你自己回退，不是正式代码。
- `archive/` 里放的是历史原型、未接路由旧页面和临时产物，不应该跟正式版一起进新的上线仓库主干。

## 你最省事的手动上传方式

如果你还是准备手动上传 GitHub，最稳妥的方式是：

1. 在本地打开 `app/`。
2. 只看 `app/` 里面的文件和文件夹。
3. 把这些内容上传到 GitHub 仓库根目录。
4. 确认远端仓库根目录能直接看到 `src/`、`public/`、`package.json`、`vite.config.ts`，而不是先看到一个 `app/` 文件夹。

## 上线前最后核对

正式推线上前，至少核对这几件事：

1. 本地在 `app/` 下执行 `npm run build` 能通过。
2. 远端仓库里是 `app/` 的内容，不是旧静态版根目录文件。
3. 如果部署到 Cloudflare，且 GitHub 仓库根目录已经是这套前端内容，那么 Root directory 留空。
4. 如果部署到 Vercel，且 GitHub 仓库根目录已经是这套前端内容，那么 Root Directory 也留空。
5. Supabase 后台的 Site URL 和 Redirect URLs 已改成你的正式公网地址。

## 这次 UI 改版的本地备份

这次正式改版前的本地快照在：

- [ui-backups/2026-05-01-pre-life-space-redesign/README.md](../ui-backups/2026-05-01-pre-life-space-redesign/README.md)

如果你想回到改版前，从那里按说明拷回 `app/` 即可。